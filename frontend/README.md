# Frontend (Vite + React + ethers + fhenixjs)

- Copy your deployed contract address into `.env`:
  ```
  VITE_CONTRACT=0xYourDeployedAddress
  ```

- Run:
  ```bash
  npm i
  npm run dev
  ```

Connect MetaMask (Helium testnet) and use the **Apply / Fetch Decision / Pay** flow.